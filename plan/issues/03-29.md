# current issues

backspace in text doesnt delete rows (if no text above)
we dont really save whitespace
backspace on certain md elements doesnt delete them, eg bullets
ctrl+z/y not working that nicely with slash commands for some reason, we need tos orta remove the whoel command from stack
